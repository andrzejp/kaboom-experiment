{
  "name": "kaboom-experiment",
  "source": "src/index.html",
  "browserslist": [
    "defaults"
  ],
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url ./",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*.js": "eslint",
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "eslint-config-prettier": "^9.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "parcel": "^2.9.3",
    "prettier": "^3.0.3"
  },
  "dependencies": {
    "kaboom": "^3000.1.8"
  }
}
